import { memo } from 'react'
import { ContentPagePublicModule } from '@/widgets/ContentPagePublicModule'

const ContentPagePublic = memo(() => {
    return <ContentPagePublicModule />
})

export default ContentPagePublic
